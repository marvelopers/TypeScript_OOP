{
  // array
  const numbers: number[] = [1,2];  // readonly 사용가능
  const strings: Array<string> = ['1','2'];

  // Tuple : 여러 타입을 데이터를 담을 수 있음, 권장하지 않음
// ㅑㅜㅅㄷㄱ
  let person:[number, string];
  
  const [name, age] = student;
}